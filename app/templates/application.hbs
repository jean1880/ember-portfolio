{{#side-menu width="4%" }}
  <ul class="nav">
    <li>
      <a class="ember-view">
      {{#side-menu-toggle}}
        {{#fa-stack size="lg" as |s|}}
          {{s.stack-2x "circle"}}
          {{s.stack-1x "chevron-left" inverse="true"}}
        {{/fa-stack}}
      {{/side-menu-toggle}}
    </a>
    </li>
    <li>
      {{#side-menu-link-to "home"}}
        {{#fa-stack size="lg" as |s|}}
          {{s.stack-2x "circle"}}
          {{s.stack-1x "home" inverse="true"}}
        {{/fa-stack}}
      {{/side-menu-link-to}}
    </li>
    <li>
      {{#side-menu-link-to "about-me"}}
        {{#fa-stack size="lg" as |s|}}
          {{s.stack-2x "circle"}}
          {{s.stack-1x "user" inverse="true"}}
        {{/fa-stack}}
      {{/side-menu-link-to}}
    </li>
  </ul>
{{/side-menu}}
<!-- Top header section -->
<header class="navbar navbar-default">
  <div class="navbar-header">
    <div class="col-xs-12 display-inline">
      {{#side-menu-toggle}}
        {{fa-icon "bars" size="3"}}
      {{/side-menu-toggle}}
      <h1>Jean-Luc Desroches<small> - Portfolio</small></h1>
    </div>
  </div>
</header><!-- //END HEADER -->

<div class="page-content">
  <main>
    <div class="container">
      {{outlet}}
    </div>
  </main>
</div>
